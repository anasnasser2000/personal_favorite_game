
<!doctype html>
<html lang="ar" dir="rtl">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<title>الرئيسية</title>
<link rel="stylesheet" href="/static/css/style.css">

<style>
.nav-badge{
    display:inline-flex;
    align-items:center;
    justify-content:center;
    min-width:20px;
    height:20px;
    padding:0 6px;
    margin-right:4px;
    border-radius:999px;
    background:#e53935;
    color:white;
    font-size:12px;
    font-weight:bold;
}
</style>

<script>
async function updateBadges(){
    try{
        const n = await fetch(
            "/api/notifications",
            {cache:"no-store"}
        ).then(r => r.json());

        const c = await fetch(
            "/api/chat/unread",
            {cache:"no-store"}
        ).then(r => r.json());

        const notificationBadge =
            document.getElementById("notificationBadge");

        const chatBadge =
            document.getElementById("chatBadge");

        if(notificationBadge){
            const count = Number(n.unread || 0);
            notificationBadge.textContent =
                count > 0 ? count : "";
            notificationBadge.style.display =
                count > 0 ? "inline-flex" : "none";
        }

        if(chatBadge){
            const count = Number(c.unread || 0);
            chatBadge.textContent =
                count > 0 ? count : "";
            chatBadge.style.display =
                count > 0 ? "inline-flex" : "none";
        }

    }catch(e){}
}

document.addEventListener(
    "DOMContentLoaded",
    updateBadges
);

setInterval(updateBadges, 3000);
</script>

</head>
<body>
<div class="game-page">
<header class="topbar">
<h2>سؤالي لي شخصي المفضل❤</h2>
<a class="owner" href="https://www.facebook.com/share/19Edkj7r9m/" target="_blank">صانعة اللعبة ❤️</a>
</header>

<main class="content">
<div class="profile-mini">
<div class="avatar">👤</div>
<div>
<strong>{{ user.name }}</strong>
<small>@{{ user.username }} • ID: {{ user.player_id }}</small>
</div>
</div>

<div class="hero">
<div class="heart">❤️</div>
<h1>جاهز للعب؟</h1>
<p>اختار طريقة اللعب وابدأ</p>
<a class="main-play" href="/mode">ابدأ اللعبة 🎮</a>
</div>
</main>

<nav class="bottom-nav">
<a href="/profile">👤<small>البروفايل</small></a>
<a href="/friends">➕<small>صديق</small></a>
<a href="/chat">💬<small>الدردشة <span id="chatBadge" class="nav-badge" style="display:none;"></span></small></a>
<a href="/friends/search">🔎<small>البحث</small></a>
</nav>
</div>




<!-- GAME_INTRO_AND_BADGES -->

<style>
.nav-badge {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-width: 19px;
    height: 19px;
    padding: 0 5px;
    margin-right: 3px;
    border-radius: 50px;
    background: #e53935;
    color: white;
    font-size: 11px;
    font-weight: 800;
    line-height: 19px;
}

.game-intro-overlay {
    position: fixed;
    inset: 0;
    z-index: 99999;
    background: rgba(0,0,0,.72);
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 18px;
}

.game-intro-box {
    width: 100%;
    max-width: 520px;
    max-height: 90vh;
    overflow-y: auto;
    background: white;
    border-radius: 24px;
    padding: 22px;
    text-align: right;
    direction: rtl;
    box-shadow: 0 15px 50px rgba(0,0,0,.3);
}

.game-intro-box h2 {
    text-align: center;
    margin-top: 0;
}

.game-intro-box p {
    line-height: 1.8;
}

.game-intro-warning {
    padding: 13px;
    border-radius: 15px;
    background: #fff3cd;
    margin: 14px 0;
    line-height: 1.8;
}

.game-intro-btn {
    width: 100%;
    border: 0;
    border-radius: 15px;
    padding: 14px;
    font-size: 16px;
    font-weight: bold;
    cursor: pointer;
}
</style>

<div id="gameIntro" class="game-intro-overlay" style="display:none;">
    <div class="game-intro-box">

        <h2>🎮 أهلاً بيك في اللعبة ❤️</h2>

        <p>
            <strong>سؤالي لي شخصي المفضل❤</strong>
            لعبة أسئلة وتحديات ممتعة تلعبها بروحك
            أو مع شخص ثاني.
        </p>

        <p>
            🎯 اختار القسم والكرت، وبعدها جاوب على الأسئلة.
            الإجابة الصحيحة تعطيك <strong>+10 نقاط</strong>
            والخطأ <strong>0 نقاط</strong>.
        </p>

        <p>
            👥 تقدر تلعب مع المرتبطين، الأصدقاء، الإخوة،
            المخطوبين أو المتزوجين، أو تلعب بروحك.
        </p>

        <p>
            💬 عندك دردشة خاصة مع أصدقائك،
            وكذلك دردشة منفصلة داخل الغرفة.
        </p>

        <div class="game-intro-warning">
            🛡️ <strong>تنبيه مهم:</strong><br>
            لأجل حماية اللاعبين وإدارة اللعبة وحل المشاكل،
            يتم تسجيل ومتابعة بعض أنشطة اللعبة.
            وقد يتم إرسال الأحداث المهمة إلى إدارة اللعبة عبر Telegram.
            وإذا كانت مراقبة الدردشة مفعّلة، فقد يتم إرسال محتوى
            الرسائل الخاصة ورسائل الغرف للإدارة.
            <br><br>
            🚫 لا ترسل كلمات المرور أو رموز التحقق أو أي معلومات
            شخصية حساسة داخل الدردشة.
            ويمنع الإساءة أو التهديد أو التحرش أو نشر أي محتوى مخالف.
        </div>

        <p>
            باستمرارك في استخدام اللعبة، أنت توافق على هذه القواعد.
        </p>

        <button
            class="game-intro-btn main-btn"
            onclick="closeGameIntro()">
            ❤️ فهمت، ابدأ اللعب 🎮
        </button>

    </div>
</div>

<script>
function closeGameIntro() {
    localStorage.setItem("personal_favorite_game_intro", "1");

    const box = document.getElementById("gameIntro");

    if (box) {
        box.style.display = "none";
    }
}

function showGameIntro() {
    if (localStorage.getItem("personal_favorite_game_intro") !== "1") {
        const box = document.getElementById("gameIntro");

        if (box) {
            box.style.display = "flex";
        }
    }
}

async function updateMainBadges() {
    try {
        const n = await fetch(
            "/api/notifications",
            {cache:"no-store"}
        ).then(r => r.json());

        const c = await fetch(
            "/api/chat/unread",
            {cache:"no-store"}
        ).then(r => r.json());

        const notificationBadge =
            document.getElementById("notificationBadge");

        const chatBadge =
            document.getElementById("chatBadge");

        if (notificationBadge && n.ok) {
            const count = Number(n.unread || 0);

            notificationBadge.textContent =
                count > 0 ? count : "";

            notificationBadge.style.display =
                count > 0 ? "inline-flex" : "none";
        }

        if (chatBadge && c.ok) {
            const count = Number(c.unread || 0);

            chatBadge.textContent =
                count > 0 ? count : "";

            chatBadge.style.display =
                count > 0 ? "inline-flex" : "none";
        }

    } catch (error) {
        console.log("badge update error");
    }
}

document.addEventListener(
    "DOMContentLoaded",
    function() {
        showGameIntro();
        updateMainBadges();
    }
);

setInterval(updateMainBadges, 2500);
</script>

</body>
</html>
